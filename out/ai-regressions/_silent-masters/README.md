# Silent masters. Not for publishing.

These are the raw Remotion renders with no voiceover. They carry a silent audio
track, so a player shows an audio channel and plays nothing, which is easy to
mistake for a broken export.

The publishable files are the `FINAL-*.mp4` in the parent folder.
